server:
  port: 8010
  servlet:
    context-path: /pets
 
spring:
 application:
   name: pet-service
 datasource:
   hikari:
     maximum-pool-size: 2
   url: ${DATABASE_URL}
   password: ${DATABASE_PASSWORD}
   username: ${DATABASE_USERNAME}

message: "hello from github"
